export const queryKeys = Object.freeze({
  appTheme: "app-theme",
  clearAppTheme: "clear-app-theme",
});

export const idxedDBNames = {
  notes: "notes",
  items: "items",
};

export const idxedDBKeys = {
  notes: {
    
  }
};
