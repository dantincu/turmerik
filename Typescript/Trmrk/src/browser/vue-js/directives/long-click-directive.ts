import { App } from "vue";
/* import { longClickDirective } from "vue-long-click";

export const createLongClickDirective = (app: App) => {
  const longClickInstance = longClickDirective({ delay: 400, interval: 50 });
  app.directive("longclick", longClickInstance);
};
*/
