import { TrmrkLongPressOrRightClick } from './trmrk-long-press-or-right-click';

describe('LongPressOrRightClick', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new TrmrkLongPressOrRightClick();
    expect(directive).toBeTruthy();
  });
});
