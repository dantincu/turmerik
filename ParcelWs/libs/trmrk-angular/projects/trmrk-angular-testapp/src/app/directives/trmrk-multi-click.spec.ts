import { TrmrkMultiClick } from '../trmrk-multi-click';

describe('TrmrkMultiClick', () => {
  it('should create an instance', () => {
    const directive = new TrmrkMultiClick();
    expect(directive).toBeTruthy();
  });
});
