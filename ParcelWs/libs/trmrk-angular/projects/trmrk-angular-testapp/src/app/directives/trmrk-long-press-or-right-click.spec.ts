import { LongPressOrRightClick } from '../long-press-or-right-click';

describe('LongPressOrRightClick', () => {
  it('should create an instance', () => {
    const directive = new LongPressOrRightClick();
    expect(directive).toBeTruthy();
  });
});
