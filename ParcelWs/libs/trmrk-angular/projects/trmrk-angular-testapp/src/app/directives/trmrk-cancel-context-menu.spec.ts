import { TrmrkCancelContextMenu } from './trmrk-cancel-context-menu';

describe('TrmrkCancelContextMenu', () => {
  it('should create an instance', () => {
    const directive = new TrmrkCancelContextMenu();
    expect(directive).toBeTruthy();
  });
});
