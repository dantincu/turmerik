import { TrmrkDrag } from './trmrk-drag';

describe('TrmrkDrag', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new TrmrkDrag();
    expect(directive).toBeTruthy();
  });
});
